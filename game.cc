#include "game.hh"

Game::Game(int size_x, int size_y, Player* p): m_size_window_x(size_x), m_size_window_y(size_y), m_player(p) {}

void Game::player_shot()
{
	// m_list_shot.push_back(m_player->shoot());
}
